#include "vyn/vre/value.hpp"
#include <stdexcept>

namespace vyn::vre {

// Implementations for VreValue methods (if any non-inline logic is needed)
// All methods are currently inline in the header, but you can move logic here if needed.

// Example: (none needed yet, as all logic is in the header)

} // namespace vyn::vre
