<!-- generated by tools/refman.py; do not edit -->
<!-- source: stdlib/ ; runtime: runtime/vyb_runtime.c ; commit: 5486af8d8c1ff7d535326b1e3562dd88e398cdbf -->

# Shared types & consumers

Cross-module structs and enums — types defined in one module but imported, built, or handled by another. For each shared type: who imports/re-exports it and which functions use it in a signature.

| Type | Module | Consumers | Consumer modules |
|---|---|---|---|
| [`HttpResponse`](http.md#sym-httpresponse) | [`http`](http.md) | 1 | [`https`](https.md) |
| [`Socket`](network.md#sym-socket) | [`network`](network.md) | 1 | [`http`](http.md) |
| [`TlsContext`](tls.md#sym-tlscontext) | [`tls`](tls.md) | 1 | [`https`](https.md) |
| [`TlsStream`](tls.md#sym-tlsstream) | [`tls`](tls.md) | 1 | [`https`](https.md) |

<a id="sym-http-httpresponse"></a>
## `HttpResponse` · struct

A parsed HTTP response. `headers` holds the raw response header lines (minus

- **Defined in** [`http`](http.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `https_get_full` uses it, `https_get_full_verified` uses it, `module` imports it (re-export)
<a id="sym-network-socket"></a>
## `Socket` · enum

Domain / socket-type constants. A constant enum: each member is a compile-time

- **Defined in** [`network`](network.md)

**Consumed by 1 module(s)**

- [`http`](http.md) — `module` imports it
<a id="sym-tls-tlscontext"></a>
## `TlsContext` · struct

A holding SSL_CTX (client or server). Handles are opaque Ints.

- **Defined in** [`tls`](tls.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `module` imports it
<a id="sym-tls-tlsstream"></a>
## `TlsStream` · struct

A TLS session bound to an already-connected fd. `ssl` is the SSL* handle;

- **Defined in** [`tls`](tls.md)

**Consumed by 1 module(s)**

- [`https`](https.md) — `module` imports it, `tls_read_all` uses it, `tls_read_exact` uses it, `tls_read_line` uses it
