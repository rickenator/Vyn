<!-- generated by tools/refman.py; do not edit -->
<!-- source: stdlib/ ; runtime: runtime/vyb_runtime.c ; commit: f92a6d22ea6ab28765b19768c8e21daa126b99fc -->

# Module `io`

io - File input/output module.

Provides the `File` value plus the `open` / `close` / `read_all` / `write_str`
syscall-backed helpers and the portable `FILE_*` open-mode helpers. All I/O
crosses the runtime's `__vyb_file_*` functions in `runtime/vyb_runtime.c`; the
Vyb surface stays allocation/pointer-free — a `File` only carries a plain
`Int` descriptor and the path it was opened on.

Usage:
  import io::{open_write, write_str, close, open_read, read_all}
  f<File> = open_write("data.txt")     # FileFlag::WRITE | CREATE | TRUNC
  write_str(f, "hello"); close(f)
  r<File> = open_read("data.txt")
  println(read_all(r)); close(r)

Diagnostic surface: `error_code()` (0 = no error) and `error_message()` report
the result of the last file operation.

## Fan-in / Fan-out

- **Referenced by** (0): —
- **References** (0): —

## Exported symbols

| Symbol | Kind | Fan-in | Summary |
|---|---|---|---|
| [`FileFlag`](#sym-fileflag) | enum |  | Open-mode helper flags (a portable bitmask understood by the runtime helper). |
| [`close`](#sym-close) | fn |  | Close an open file. Returns 0 on success, -1 on failure. |
| [`error_code`](#sym-error_code) | fn |  | Numeric error code of the last file operation (0 = no error). |
| [`error_message`](#sym-error_message) | fn |  | Human-readable message for the last file operation error. |
| [`io_status_message`](#sym-io_status_message) | fn |  | Compatibility status probe (kept for the module-surface import test). |
| [`open`](#sym-open) | fn |  | Open (or create) `path` with the given `FileFlag` flags. Returns the `File`; test |
| [`open_append`](#sym-open_append) | fn |  | Convenience: open for appending, creating the file if absent. |
| [`open_read`](#sym-open_read) | fn |  | Convenience: open an existing file for reading. |
| [`open_write`](#sym-open_write) | fn |  | Convenience: create/truncate a file for writing. |
| [`read_all`](#sym-read_all) | fn |  | Read the whole file into a `String`. Returns an empty String on failure (check |
| [`write_str`](#sym-write_str) | fn |  | Write `s` to the file. Returns the number of bytes written, or -1 on failure. |
| [`File`](#sym-file) | struct |  | An open file. `fd` is -1 when the last open failed; `path` records the target. |

<a id="sym-fileflag"></a>
### `FileFlag` · enum

```
enum FileFlag {
```

Variants:

- `READ = 1`
- `WRITE = 2`
- `RDWR = 4`
- `CREATE = 128`
- `TRUNC = 512`
- `APPEND = 1024`

Open-mode helper flags (a portable bitmask understood by the runtime helper).
A constant enum: `FileFlag::READ` is an `Int` and combines with `|`.

<a id="sym-close"></a>
### `close` · fn

```
close(f<File>)<Int> ->
```

Close an open file. Returns 0 on success, -1 on failure.

**Relationships**

- uses type [`File`](#sym-file)

<a id="sym-error_code"></a>
### `error_code` · fn

```
error_code()<Int> ->
```

Numeric error code of the last file operation (0 = no error).

<a id="sym-error_message"></a>
### `error_message` · fn

```
error_message()<String> ->
```

Human-readable message for the last file operation error.

<a id="sym-io_status_message"></a>
### `io_status_message` · fn

```
io_status_message()<String> ->
```

Compatibility status probe (kept for the module-surface import test).

<a id="sym-open"></a>
### `open` · fn

```
open(path<String>, flags<Int>)<File> ->
```

Open (or create) `path` with the given `FileFlag` flags. Returns the `File`; test
`fd >= 0` (or `error_code()`) to detect failure.

**Relationships**

- mentions [`File`](#sym-file)
- mentions [`FileFlag`](#sym-fileflag)
- mentions [`error_code`](#sym-error_code)
- uses type [`File`](#sym-file)

<a id="sym-open_append"></a>
### `open_append` · fn

```
open_append(path<String>)<File> ->
```

Convenience: open for appending, creating the file if absent.

**Relationships**

- calls [`open`](#sym-open)
- uses type [`File`](#sym-file)

<a id="sym-open_read"></a>
### `open_read` · fn

```
open_read(path<String>)<File> ->
```

Convenience: open an existing file for reading.

**Relationships**

- calls [`open`](#sym-open)
- uses type [`File`](#sym-file)

<a id="sym-open_write"></a>
### `open_write` · fn

```
open_write(path<String>)<File> ->
```

Convenience: create/truncate a file for writing.

**Relationships**

- calls [`open`](#sym-open)
- uses type [`File`](#sym-file)

<a id="sym-read_all"></a>
### `read_all` · fn

```
read_all(f<File>)<String> ->
```

Read the whole file into a `String`. Returns an empty String on failure (check
`error_code()` to distinguish an empty file from an error).

**Relationships**

- mentions [`error_code`](#sym-error_code)
- uses type [`File`](#sym-file)

<a id="sym-write_str"></a>
### `write_str` · fn

```
write_str(f<File>, s<String>)<Int> ->
```

Write `s` to the file. Returns the number of bytes written, or -1 on failure.

**Relationships**

- uses type [`File`](#sym-file)

<a id="sym-file"></a>
### `File` · struct

```
struct File {
```

Fields:

- `fd<Int>`
- `path<String>`

An open file. `fd` is -1 when the last open failed; `path` records the target.
