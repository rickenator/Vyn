<!-- generated by tools/refman.py; do not edit -->
<!-- source: stdlib/ ; runtime: runtime/vyb_runtime.c ; commit: 25b9f3a27f71904dd6d2336801a569b0102285b3 -->

# Module `qt`

qt - Qt5 Widgets native GUI, wrapped as an opaque-Int surface.

A small, deterministic subset of Qt5 Widgets for native-window programs. The
C++ QApplication/QWidget/QLabel object model stays in the runtime
(`__vyb_qt_*` shims in `runtime/vyb_qt_bridge.cpp`); Vyb sees only Int
handles (qintptr-sized), Int dimensions, Bool/Int status flags, and String
text. String arguments cross as UTF-8 byte buffers; the title/label-text
getters return owned Strings.

Lifecycle: call qt_init() once. QApplication must live on the main thread,
which is where a Vyb program's main runs. Construction needs a Qt platform -
under X11/xcb with a display, offscreen, etc. On a headless box with neither
a QPA platform nor a $DISPLAY the bridge falls back to the `offscreen`
platform so qt_init() stays deterministic; GUI code is still expected to run
where a GUI is available.

Two event-loop models coexist. The default is *polled*: call
qt_process_events() to pump paint/timer/input events, arm a polled timer with
qt_set_timer(ms) then check qt_timer_fired() (it clears on read), and drain
control signals with qt_event_count/handle/kind/pop (or qt_wait_event). This
stays deterministic under xvfb/offscreen and fits the Int/String FFI.

For a GUI program, qt_run() enters Qt's own native loop (QApplication::exec())
and dispatches queued control events into the handler registered with
qt_on_event(fn(handle, kind)); stop it from a handler with qt_run_stop() (whose
GUI survives) or qt_quit() (full teardown after the loop returns). Background
asyncs keep running on their worker threads and their enqueued results reach
the handler through the same queue-draining loop.

An optional web surface (`qt_web_*`, QWebEngineView) sits on top of Widgets and
is only usable when QtWebEngine is linked into the build (CMake finds
Qt5WebEngineWidgets). With QtWebEngine absent the surface resolves to stub
shims: qt_web_create() returns 0 and the other qt_web_* calls return their
documented defaults, so web-capable programs degrade gracefully. Loading a URL
is asynchronous; loadFinished/titleChanged/loadProgress enqueue the shared
QtEvent kinds and flow through the same qt_on_event / qt_event_* dispatch.

Everything returns 0 on success and -1 on error (handle lookups fail on a 0/
stale or non-matching handle; operation with no QApplication returns -1),
except the documented deviations: qt_init/qt_active/qt_window_visible/
qt_timer_fired return 1/0 booleans, qt_window_create/qt_label_create return
a fresh Int handle (0 on failure), and qt_window_width/height return the
current size (or -1 on a bad handle).

## Fan-in / Fan-out

- **Referenced by** (0): —
- **References** (0): —

## Exported symbols

| Symbol | Kind | Fan-in | Summary |
|---|---|---|---|
| [`QtEvent`](#sym-qtevent) | enum |  | gen_qt[mod_enums]: begin |
| [`QtWidgetKind`](#sym-qtwidgetkind) | enum |  |  |
| [`qt_action_add`](#sym-qt_action_add) | fn |  | Add an action to `menu`; its trigger enqueues QtEvent::Click. Returns the action handle. |
| [`qt_action_count`](#sym-qt_action_count) | fn |  | Number of actions in `menu`, or -1 on a bad handle. |
| [`qt_active`](#sym-qt_active) | fn |  | true while the GUI is initialized. |
| [`qt_button_create`](#sym-qt_button_create) | fn |  | Create a push button under `parent`; a click enqueues QtEvent::Click. Returns its handle. |
| [`qt_button_set_enabled`](#sym-qt_button_set_enabled) | fn |  | Enable (true) / disable (false) the button. Returns 0. |
| [`qt_button_set_text`](#sym-qt_button_set_text) | fn |  | Replace button label. Returns 0, or -1 on a non-button handle. |
| [`qt_button_text`](#sym-qt_button_text) | fn |  | Current button label (String); "" on a non-button handle. |
| [`qt_checkbox_checked`](#sym-qt_checkbox_checked) | fn |  | true if the checkbox is checked, else false. |
| [`qt_checkbox_create`](#sym-qt_checkbox_create) | fn |  | Create a checkbox under `parent`; toggles enqueue QtEvent::Toggled. Returns its handle. |
| [`qt_checkbox_set_checked`](#sym-qt_checkbox_set_checked) | fn |  | Check (true) / uncheck (false) the box (enqueues QtEvent::Toggled). Returns 0. |
| [`qt_combo_add_item`](#sym-qt_combo_add_item) | fn |  | Append `text` as the last combo item. Returns 0. |
| [`qt_combo_count`](#sym-qt_combo_count) | fn |  | Combo item count, or -1 on a bad handle. |
| [`qt_combo_create`](#sym-qt_combo_create) | fn |  | Create a combo box under `parent`; index changes enqueue QtEvent::IndexChanged. |
| [`qt_combo_current_index`](#sym-qt_combo_current_index) | fn |  | Currently selected combo index (0-based), or -1 on a bad handle. |
| [`qt_combo_item_text`](#sym-qt_combo_item_text) | fn |  | Text of combo item `idx` (String); "" on a bad handle/index. |
| [`qt_combo_set_current_index`](#sym-qt_combo_set_current_index) | fn |  | Select combo item `idx`. Returns 0. |
| [`qt_dial_create`](#sym-qt_dial_create) | fn |  | Create a dial with range [min, max]; value changes enqueue QtEvent::ValueChanged. |
| [`qt_dial_set_value`](#sym-qt_dial_set_value) | fn |  | Set the dial value (clamped). Returns 0. |
| [`qt_dial_value`](#sym-qt_dial_value) | fn |  | Current dial value, or 0 on a bad handle. |
| [`qt_dir_select`](#sym-qt_dir_select) | fn |  | Modal directory picker; returns the chosen path (String) or "" on cancel. |
| [`qt_dlg_about`](#sym-qt_dlg_about) | fn |  | Open a non-blocking about box; returns its handle, or 0 on failure. |
| [`qt_dlg_close`](#sym-qt_dlg_close) | fn |  | Finish dialog `h` as rejected (enqueues QtEvent::dialog with result 0). Returns 0. |
| [`qt_dlg_dir`](#sym-qt_dlg_dir) | fn |  | Open a non-blocking directory picker; finishes with result 1 (Accepted) + qt_dlg_selected. |
| [`qt_dlg_error`](#sym-qt_dlg_error) | fn |  | Open a non-blocking error box; returns its handle, or 0 on failure. |
| [`qt_dlg_info`](#sym-qt_dlg_info) | fn |  | Open a non-blocking information box; returns its handle, or 0 on failure. |
| [`qt_dlg_open`](#sym-qt_dlg_open) | fn |  | Open a non-blocking file-open picker; finishes with result 1 (Accepted) + qt_dlg_selected. |
| [`qt_dlg_question`](#sym-qt_dlg_question) | fn |  | Open a non-blocking Yes/No box; finishes with result 1 for Yes, 0 otherwise. |
| [`qt_dlg_save`](#sym-qt_dlg_save) | fn |  | Open a non-blocking file-save picker; finishes with result 1 (Accepted) + qt_dlg_selected. |
| [`qt_dlg_selected`](#sym-qt_dlg_selected) | fn |  | Path chosen by file/dir dialog `h` (String); "" on a non-picker handle. |
| [`qt_dlg_warn`](#sym-qt_dlg_warn) | fn |  | Open a non-blocking warning box; returns its handle, or 0 on failure. |
| [`qt_edit_create`](#sym-qt_edit_create) | fn |  | Create a single-line text edit under `parent`; text changes enqueue QtEvent::TextChanged. |
| [`qt_edit_set_placeholder`](#sym-qt_edit_set_placeholder) | fn |  | Set placeholder (ghost) text. Returns 0. |
| [`qt_edit_set_text`](#sym-qt_edit_set_text) | fn |  | Replace edit text. Returns 0, or -1 on a non-edit handle. |
| [`qt_edit_text`](#sym-qt_edit_text) | fn |  | Current edit text (String); "" on a non-edit handle. |
| [`qt_event_count`](#sym-qt_event_count) | fn |  | Number of pending control events queued since the last drains. |
| [`qt_event_handle`](#sym-qt_event_handle) | fn |  | Handle of the oldest queued event's widget (0 if empty). |
| [`qt_event_kind`](#sym-qt_event_kind) | fn |  | Kind of the oldest queued event (QtEvent); 0 if empty. |
| [`qt_event_pop`](#sym-qt_event_pop) | fn |  | Remove the oldest queued event. Returns 0, or -1 if empty. |
| [`qt_event_result`](#sym-qt_event_result) | fn |  | Result payload of the oldest queued dialog event, or of the event being dispatched by qt_run (1 = Yes/Accepted, else 0). |
| [`qt_file_open`](#sym-qt_file_open) | fn |  | Modal 'open file' picker; returns the chosen path (String) or "" on cancel. |
| [`qt_file_save`](#sym-qt_file_save) | fn |  | Modal 'save file' picker; returns the chosen path (String) or "" on cancel. |
| [`qt_grid`](#sym-qt_grid) | fn |  | Create a grid layout on window `parent`. Returns a layout handle. |
| [`qt_grid_add`](#sym-qt_grid_add) | fn |  | Add widget `child` to grid-layout `layout` at (row, col). Returns 0. |
| [`qt_group_create`](#sym-qt_group_create) | fn |  | Create a titled group-box container under `parent`; put a layout on it. Returns its handle or 0. |
| [`qt_hbox`](#sym-qt_hbox) | fn |  | Create a horizontal box layout on window `parent`. Returns a layout handle. |
| [`qt_init`](#sym-qt_init) | fn |  | Initialize the QApplication (once); true if the GUI is available. |
| [`qt_kind`](#sym-qt_kind) | fn |  | Static widget kind (QtWidgetKind) of handle `h`, or 0 if not a widget. |
| [`qt_label_create`](#sym-qt_label_create) | fn |  | Create a label with `text` under `parent` (0 = none); returns its handle or 0. |
| [`qt_label_set_text`](#sym-qt_label_set_text) | fn |  | Replace label text. Returns 0, or -1 on a non-label handle. |
| [`qt_label_text`](#sym-qt_label_text) | fn |  | Current label text (String); "" on a non-label handle. |
| [`qt_layout_add`](#sym-qt_layout_add) | fn |  | Add widget `child` to box-layout `layout`. Returns 0. |
| [`qt_layout_add_layout`](#sym-qt_layout_add_layout) | fn |  | Nest sub-layout `sub` inside box-layout `layout` (adds it as the next item). Returns 0. |
| [`qt_layout_set_stretch`](#sym-qt_layout_set_stretch) | fn |  | Set the stretch factor of the item at `index` (add order, 0-based) to `stretch`. Returns 0. |
| [`qt_list_add`](#sym-qt_list_add) | fn |  | Append `text` as the last list item. Returns 0. |
| [`qt_list_count`](#sym-qt_list_count) | fn |  | List item count, or -1 on a bad handle. |
| [`qt_list_create`](#sym-qt_list_create) | fn |  | Create an item list under `parent`; selection changes enqueue QtEvent::CurrentChanged. Returns its handle. |
| [`qt_list_current`](#sym-qt_list_current) | fn |  | Current (0-based) list index, or -1 when none / bad handle. |
| [`qt_list_item_text`](#sym-qt_list_item_text) | fn |  | Text of list item `idx` (String); "" on a bad handle/index. |
| [`qt_list_set_current`](#sym-qt_list_set_current) | fn |  | Select list item `idx`. Returns 0. |
| [`qt_main_window_create`](#sym-qt_main_window_create) | fn |  | Create a top-level QMainWindow (kind Window) with menubar/statusbar/toolbar support; returns its handle or 0. |
| [`qt_menu_add`](#sym-qt_menu_add) | fn |  | Add a top-level menu titled `title` to the menu bar; returns its menu handle or 0. |
| [`qt_menubar`](#sym-qt_menubar) | fn |  | The main window's menu bar handle (created on demand). |
| [`qt_msg_about`](#sym-qt_msg_about) | fn |  | Show a modal 'about' box (blocks until dismissed). Returns 0. |
| [`qt_msg_error`](#sym-qt_msg_error) | fn |  | Show a modal critical-error message box (blocks until dismissed). Returns 0. |
| [`qt_msg_info`](#sym-qt_msg_info) | fn |  | Show a modal informational message box (blocks until dismissed). Returns 0. |
| [`qt_msg_question`](#sym-qt_msg_question) | fn |  | Show a modal Yes/No question. Returns 1 for Yes, 0 for No/dismiss. |
| [`qt_msg_warn`](#sym-qt_msg_warn) | fn |  | Show a modal warning message box (blocks until dismissed). Returns 0. |
| [`qt_on_event`](#sym-qt_on_event) | fn |  | Register fn(handle, kind) called for each control event queued while qt_run() runs. Returns 0. |
| [`qt_post_event`](#sym-qt_post_event) | fn |  | Enqueue a synthetic control event for handle `h` (thread-safe: a background async can signal the UI loop without touching widgets off-thread). Returns 0. |
| [`qt_process_events`](#sym-qt_process_events) | fn |  | Pump the Qt event loop once. Returns 0, or -1 if the GUI is not running. |
| [`qt_progress_create`](#sym-qt_progress_create) | fn |  | Create a progress bar with range 0..max under `parent`. Returns its handle or 0. |
| [`qt_progress_set_value`](#sym-qt_progress_set_value) | fn |  | Set the progress value (clamped). Returns 0. |
| [`qt_quit`](#sym-qt_quit) | fn |  | Shut the GUI down (terminal - do not use Qt handles afterwards). Returns 0. |
| [`qt_radio_checked`](#sym-qt_radio_checked) | fn |  | true if the radio button is checked, else false. |
| [`qt_radio_create`](#sym-qt_radio_create) | fn |  | Create a radio button under `parent`; toggles enqueue QtEvent::Toggled. Returns its handle. |
| [`qt_radio_set_checked`](#sym-qt_radio_set_checked) | fn |  | Check (true) / uncheck (false) the radio (enqueues QtEvent::Toggled). Returns 0. |
| [`qt_rich_append`](#sym-qt_rich_append) | fn |  | Append `text` at the end (keeps the current character format). Returns 0. |
| [`qt_rich_clear`](#sym-qt_rich_clear) | fn |  | Clear all rich-text content. Returns 0. |
| [`qt_rich_create`](#sym-qt_rich_create) | fn |  | Create a rich-text editor (QTextEdit) under `parent`; edits enqueue QtEvent::TextChanged. |
| [`qt_rich_html`](#sym-qt_rich_html) | fn |  | Current rich-text body as HTML (String); "" on a bad handle. |
| [`qt_rich_plain`](#sym-qt_rich_plain) | fn |  | Current plain text (String); "" on a bad handle. |
| [`qt_rich_set_html`](#sym-qt_rich_set_html) | fn |  | Set the rich-text editor body from `html` (supports <b>/<i>/<font color> etc). Returns 0. |
| [`qt_rich_set_plain`](#sym-qt_rich_set_plain) | fn |  | Set the rich-text editor's plain text (clears formatting). Returns 0. |
| [`qt_rich_set_text_color`](#sym-qt_rich_set_text_color) | fn |  | Set the editor's text color to (r,g,b) each 0-255. Returns 0. |
| [`qt_run`](#sym-qt_run) | fn |  | Run the Qt native event loop, dispatching queued control events to the qt_on_event handler, until qt_run_stop()/qt_quit(). Returns exit code. |
| [`qt_run_stop`](#sym-qt_run_stop) | fn |  | Stop a running qt_run() loop (graceful; GUI stays up). Returns 0, or -1 if not running. |
| [`qt_screen_dpi`](#sym-qt_screen_dpi) | fn |  | Primary screen device-pixel-ratio scaled to 96dpi (100 = 1.0x), or 100 if no screen. |
| [`qt_screen_height`](#sym-qt_screen_height) | fn |  | Primary screen height in logical pixels, or -1 if no screen. |
| [`qt_screen_width`](#sym-qt_screen_width) | fn |  | Primary screen width in logical pixels, or -1 if no screen. |
| [`qt_set_timer`](#sym-qt_set_timer) | fn |  | Arm a repeating timer every `ms` ms; poll qt_timer_fired(). Returns 0. |
| [`qt_slider_create`](#sym-qt_slider_create) | fn |  | Create a horizontal slider with range [min, max]; value changes enqueue QtEvent::ValueChanged. |
| [`qt_slider_set_value`](#sym-qt_slider_set_value) | fn |  | Set the slider value (clamped). Returns 0. |
| [`qt_slider_value`](#sym-qt_slider_value) | fn |  | Current slider value, or 0 on a bad handle. |
| [`qt_spin_create`](#sym-qt_spin_create) | fn |  | Create an integer spin box with range [min, max]; value changes enqueue QtEvent::ValueChanged. |
| [`qt_spin_set_value`](#sym-qt_spin_set_value) | fn |  | Set the spin box value (clamped). Returns 0. |
| [`qt_spin_value`](#sym-qt_spin_value) | fn |  | Current spin box value, or 0 on a bad handle. |
| [`qt_statusbar_message`](#sym-qt_statusbar_message) | fn |  | Show `text` in the main window's status bar. Returns 0, or -1 on a bad handle. |
| [`qt_statusbar_text`](#sym-qt_statusbar_text) | fn |  | Current status-bar message (String); "" on a bad handle. |
| [`qt_tabs_add`](#sym-qt_tabs_add) | fn |  | Append a tab titled `text`; returns the page's widget handle (put a layout on it). |
| [`qt_tabs_count`](#sym-qt_tabs_count) | fn |  | Tab count, or -1 on a bad handle. |
| [`qt_tabs_create`](#sym-qt_tabs_create) | fn |  | Create a tab container under `parent`; returns its handle or 0. |
| [`qt_tabs_current`](#sym-qt_tabs_current) | fn |  | Current (0-based) tab index, or -1 on a bad handle. |
| [`qt_tabs_set_current`](#sym-qt_tabs_set_current) | fn |  | Select tab `idx` (enqueues QtEvent::CurrentChanged). Returns 0. |
| [`qt_text_edit_create`](#sym-qt_text_edit_create) | fn |  | Create a multi-line plain-text editor under `parent`; edits enqueue QtEvent::TextChanged. Returns its handle. |
| [`qt_text_edit_set_text`](#sym-qt_text_edit_set_text) | fn |  | Replace editor text. Returns 0, or -1 on a non-editor handle. |
| [`qt_text_edit_text`](#sym-qt_text_edit_text) | fn |  | Current editor text (String); "" on a bad handle. |
| [`qt_timer_fired`](#sym-qt_timer_fired) | fn |  | true once the armed timer has fired since the last check (read clears). |
| [`qt_toolbar_create`](#sym-qt_toolbar_create) | fn |  | Add a toolbar titled `title` to the main window; returns its handle or 0. |
| [`qt_vbox`](#sym-qt_vbox) | fn |  | Create a vertical box layout on window `parent`. Returns a layout handle. |
| [`qt_wait_event`](#sym-qt_wait_event) | fn |  | Pump Qt until a control event (true) or `timeout` ms lapses (false); negative waits. |
| [`qt_web_back`](#sym-qt_web_back) | fn |  | Go back in history. Returns 0, or -1 on a bad handle. |
| [`qt_web_create`](#sym-qt_web_create) | fn |  | gen_qt[mod_wrappers]: begin |
| [`qt_web_forward`](#sym-qt_web_forward) | fn |  | Go forward in history. Returns 0, or -1 on a bad handle. |
| [`qt_web_load`](#sym-qt_web_load) | fn |  | Begin loading `url` in the web view (async). Returns 0, or -1 on a bad handle. |
| [`qt_web_loading`](#sym-qt_web_loading) | fn |  | true while the page is still loading. |
| [`qt_web_reload`](#sym-qt_web_reload) | fn |  | Reload the page. Returns 0, or -1 on a bad handle. |
| [`qt_web_title`](#sym-qt_web_title) | fn |  | Current page title (String); "" on a bad handle. |
| [`qt_web_url`](#sym-qt_web_url) | fn |  | Current page URL (String); "" on a bad handle. |
| [`qt_web_zoom_in`](#sym-qt_web_zoom_in) | fn |  | Zoom the web view in (larger text). Returns 0, or -1 on a bad handle. |
| [`qt_web_zoom_out`](#sym-qt_web_zoom_out) | fn |  | Zoom the web view out (smaller text). Returns 0, or -1 on a bad handle. |
| [`qt_widget_enabled`](#sym-qt_widget_enabled) | fn |  | true while widget `h` is enabled, else false. |
| [`qt_widget_set_enabled`](#sym-qt_widget_set_enabled) | fn |  | Enable (true) / disable (false) any widget. Returns 0, or -1 on a non-widget handle. |
| [`qt_widget_set_font_bold`](#sym-qt_widget_set_font_bold) | fn |  | Toggle a widget's font bold (on != 0). Returns 0, or -1 on a bad handle. |
| [`qt_widget_set_font_size`](#sym-qt_widget_set_font_size) | fn |  | Set a widget's font point size. Returns 0, or -1 on a bad handle. |
| [`qt_widget_set_text_color`](#sym-qt_widget_set_text_color) | fn |  | Set a widget's foreground text color via palette (r,g,b each 0-255). Returns 0, or -1 on a bad handle. |
| [`qt_widget_set_visible`](#sym-qt_widget_set_visible) | fn |  | Show (true) / hide (false) any widget. Returns 0, or -1 on a non-widget handle. |
| [`qt_widget_visible`](#sym-qt_widget_visible) | fn |  | true while widget `h` is visible, else false. |
| [`qt_window_close`](#sym-qt_window_close) | fn |  | Destroy window `w` (children go with it). Returns 0. |
| [`qt_window_create`](#sym-qt_window_create) | fn |  | Create a top-level window; returns its Int handle or 0 on failure. |
| [`qt_window_height`](#sym-qt_window_height) | fn |  | Content height in pixels, or -1 on a bad handle. |
| [`qt_window_hide`](#sym-qt_window_hide) | fn |  | Hide (unmap) the window. Returns 0. |
| [`qt_window_resize`](#sym-qt_window_resize) | fn |  | Resize to (width, height) pixels. Returns 0. |
| [`qt_window_set_title`](#sym-qt_window_set_title) | fn |  | Set titlebar text. Returns 0, or -1 on a bad handle. |
| [`qt_window_show`](#sym-qt_window_show) | fn |  | Show (map) the window. Returns 0. |
| [`qt_window_title`](#sym-qt_window_title) | fn |  | Current titlebar text (String); "" on a bad handle. |
| [`qt_window_visible`](#sym-qt_window_visible) | fn |  | true while window `w` is visible, else false. |
| [`qt_window_width`](#sym-qt_window_width) | fn |  | Content width in pixels, or -1 on a bad handle. |

<a id="sym-qtevent"></a>
### `QtEvent` · enum

```
enum QtEvent {
```

Variants:

- `none = 0`
- `click = 1`
- `textChanged = 2`
- `toggled = 3`
- `indexChanged = 4`
- `valueChanged = 5`
- `loadFinished = 6`
- `titleChanged = 7`
- `loadProgress = 8`
- `currentChanged = 9`
- `dialog = 10`
- `editReturn = 11`
- `zoomIn = 12`
- `zoomOut = 13`

gen_qt[mod_enums]: begin

<a id="sym-qtwidgetkind"></a>
### `QtWidgetKind` · enum

```
enum QtWidgetKind {
```

Variants:

- `none = 0`
- `window = 1`
- `label = 2`
- `button = 3`
- `edit = 4`
- `checkbox = 5`
- `progress = 6`
- `combo = 7`
- `spin = 8`
- `slider = 9`
- `dial = 10`
- `group = 11`
- `textEdit = 12`
- `radio = 13`
- `web = 14`
- `tabs = 15`
- `list = 16`
- `menuBar = 17`
- `menu = 18`
- `toolbar = 19`
- `rich = 20`

<a id="sym-qt_action_add"></a>
### `qt_action_add` · fn

```
qt_action_add(menu<Int>, text<String>)<Int> ->
```

Add an action to `menu`; its trigger enqueues QtEvent::Click. Returns the action handle.

<a id="sym-qt_action_count"></a>
### `qt_action_count` · fn

```
qt_action_count(menu<Int>)<Int> ->
```

Number of actions in `menu`, or -1 on a bad handle.

<a id="sym-qt_active"></a>
### `qt_active` · fn

```
qt_active()<Bool> ->
```

true while the GUI is initialized.

<a id="sym-qt_button_create"></a>
### `qt_button_create` · fn

```
qt_button_create(parent<Int>, text<String>)<Int> ->
```

Create a push button under `parent`; a click enqueues QtEvent::Click. Returns its handle.

<a id="sym-qt_button_set_enabled"></a>
### `qt_button_set_enabled` · fn

```
qt_button_set_enabled(button<Int>, enabled<Bool>)<Int> ->
```

Enable (true) / disable (false) the button. Returns 0.

<a id="sym-qt_button_set_text"></a>
### `qt_button_set_text` · fn

```
qt_button_set_text(button<Int>, text<String>)<Int> ->
```

Replace button label. Returns 0, or -1 on a non-button handle.

<a id="sym-qt_button_text"></a>
### `qt_button_text` · fn

```
qt_button_text(button<Int>)<String> ->
```

Current button label (String); "" on a non-button handle.

<a id="sym-qt_checkbox_checked"></a>
### `qt_checkbox_checked` · fn

```
qt_checkbox_checked(box<Int>)<Bool> ->
```

true if the checkbox is checked, else false.

<a id="sym-qt_checkbox_create"></a>
### `qt_checkbox_create` · fn

```
qt_checkbox_create(parent<Int>, text<String>)<Int> ->
```

Create a checkbox under `parent`; toggles enqueue QtEvent::Toggled. Returns its handle.

<a id="sym-qt_checkbox_set_checked"></a>
### `qt_checkbox_set_checked` · fn

```
qt_checkbox_set_checked(box<Int>, on<Bool>)<Int> ->
```

Check (true) / uncheck (false) the box (enqueues QtEvent::Toggled). Returns 0.

<a id="sym-qt_combo_add_item"></a>
### `qt_combo_add_item` · fn

```
qt_combo_add_item(combo<Int>, text<String>)<Int> ->
```

Append `text` as the last combo item. Returns 0.

<a id="sym-qt_combo_count"></a>
### `qt_combo_count` · fn

```
qt_combo_count(combo<Int>)<Int> ->
```

Combo item count, or -1 on a bad handle.

<a id="sym-qt_combo_create"></a>
### `qt_combo_create` · fn

```
qt_combo_create(parent<Int>)<Int> ->
```

Create a combo box under `parent`; index changes enqueue QtEvent::IndexChanged.

<a id="sym-qt_combo_current_index"></a>
### `qt_combo_current_index` · fn

```
qt_combo_current_index(combo<Int>)<Int> ->
```

Currently selected combo index (0-based), or -1 on a bad handle.

<a id="sym-qt_combo_item_text"></a>
### `qt_combo_item_text` · fn

```
qt_combo_item_text(combo<Int>, idx<Int>)<String> ->
```

Text of combo item `idx` (String); "" on a bad handle/index.

<a id="sym-qt_combo_set_current_index"></a>
### `qt_combo_set_current_index` · fn

```
qt_combo_set_current_index(combo<Int>, idx<Int>)<Int> ->
```

Select combo item `idx`. Returns 0.

<a id="sym-qt_dial_create"></a>
### `qt_dial_create` · fn

```
qt_dial_create(parent<Int>, min<Int>, max<Int>)<Int> ->
```

Create a dial with range [min, max]; value changes enqueue QtEvent::ValueChanged.

<a id="sym-qt_dial_set_value"></a>
### `qt_dial_set_value` · fn

```
qt_dial_set_value(dial<Int>, value<Int>)<Int> ->
```

Set the dial value (clamped). Returns 0.

<a id="sym-qt_dial_value"></a>
### `qt_dial_value` · fn

```
qt_dial_value(dial<Int>)<Int> ->
```

Current dial value, or 0 on a bad handle.

<a id="sym-qt_dir_select"></a>
### `qt_dir_select` · fn

```
qt_dir_select(parent<Int>, title<String>)<String> ->
```

Modal directory picker; returns the chosen path (String) or "" on cancel.

<a id="sym-qt_dlg_about"></a>
### `qt_dlg_about` · fn

```
qt_dlg_about(parent<Int>, title<String>, text<String>)<Int> ->
```

Open a non-blocking about box; returns its handle, or 0 on failure.

<a id="sym-qt_dlg_close"></a>
### `qt_dlg_close` · fn

```
qt_dlg_close(h<Int>)<Int> ->
```

Finish dialog `h` as rejected (enqueues QtEvent::dialog with result 0). Returns 0.

<a id="sym-qt_dlg_dir"></a>
### `qt_dlg_dir` · fn

```
qt_dlg_dir(parent<Int>, title<String>)<Int> ->
```

Open a non-blocking directory picker; finishes with result 1 (Accepted) + qt_dlg_selected.

<a id="sym-qt_dlg_error"></a>
### `qt_dlg_error` · fn

```
qt_dlg_error(parent<Int>, title<String>, text<String>)<Int> ->
```

Open a non-blocking error box; returns its handle, or 0 on failure.

<a id="sym-qt_dlg_info"></a>
### `qt_dlg_info` · fn

```
qt_dlg_info(parent<Int>, title<String>, text<String>)<Int> ->
```

Open a non-blocking information box; returns its handle, or 0 on failure.

<a id="sym-qt_dlg_open"></a>
### `qt_dlg_open` · fn

```
qt_dlg_open(parent<Int>, title<String>, filter<String>)<Int> ->
```

Open a non-blocking file-open picker; finishes with result 1 (Accepted) + qt_dlg_selected.

<a id="sym-qt_dlg_question"></a>
### `qt_dlg_question` · fn

```
qt_dlg_question(parent<Int>, title<String>, text<String>)<Int> ->
```

Open a non-blocking Yes/No box; finishes with result 1 for Yes, 0 otherwise.

<a id="sym-qt_dlg_save"></a>
### `qt_dlg_save` · fn

```
qt_dlg_save(parent<Int>, title<String>, filter<String>)<Int> ->
```

Open a non-blocking file-save picker; finishes with result 1 (Accepted) + qt_dlg_selected.

<a id="sym-qt_dlg_selected"></a>
### `qt_dlg_selected` · fn

```
qt_dlg_selected(h<Int>)<String> ->
```

Path chosen by file/dir dialog `h` (String); "" on a non-picker handle.

<a id="sym-qt_dlg_warn"></a>
### `qt_dlg_warn` · fn

```
qt_dlg_warn(parent<Int>, title<String>, text<String>)<Int> ->
```

Open a non-blocking warning box; returns its handle, or 0 on failure.

<a id="sym-qt_edit_create"></a>
### `qt_edit_create` · fn

```
qt_edit_create(parent<Int>, text<String>)<Int> ->
```

Create a single-line text edit under `parent`; text changes enqueue QtEvent::TextChanged.

<a id="sym-qt_edit_set_placeholder"></a>
### `qt_edit_set_placeholder` · fn

```
qt_edit_set_placeholder(edit<Int>, text<String>)<Int> ->
```

Set placeholder (ghost) text. Returns 0.

<a id="sym-qt_edit_set_text"></a>
### `qt_edit_set_text` · fn

```
qt_edit_set_text(edit<Int>, text<String>)<Int> ->
```

Replace edit text. Returns 0, or -1 on a non-edit handle.

<a id="sym-qt_edit_text"></a>
### `qt_edit_text` · fn

```
qt_edit_text(edit<Int>)<String> ->
```

Current edit text (String); "" on a non-edit handle.

<a id="sym-qt_event_count"></a>
### `qt_event_count` · fn

```
qt_event_count()<Int> ->
```

Number of pending control events queued since the last drains.

<a id="sym-qt_event_handle"></a>
### `qt_event_handle` · fn

```
qt_event_handle()<Int> ->
```

Handle of the oldest queued event's widget (0 if empty).

<a id="sym-qt_event_kind"></a>
### `qt_event_kind` · fn

```
qt_event_kind()<Int> ->
```

Kind of the oldest queued event (QtEvent); 0 if empty.

<a id="sym-qt_event_pop"></a>
### `qt_event_pop` · fn

```
qt_event_pop()<Int> ->
```

Remove the oldest queued event. Returns 0, or -1 if empty.

<a id="sym-qt_event_result"></a>
### `qt_event_result` · fn

```
qt_event_result()<Int> ->
```

Result payload of the oldest queued dialog event, or of the event being dispatched by qt_run (1 = Yes/Accepted, else 0).

<a id="sym-qt_file_open"></a>
### `qt_file_open` · fn

```
qt_file_open(parent<Int>, title<String>, filter<String>)<String> ->
```

Modal 'open file' picker; returns the chosen path (String) or "" on cancel.

<a id="sym-qt_file_save"></a>
### `qt_file_save` · fn

```
qt_file_save(parent<Int>, title<String>, filter<String>)<String> ->
```

Modal 'save file' picker; returns the chosen path (String) or "" on cancel.

<a id="sym-qt_grid"></a>
### `qt_grid` · fn

```
qt_grid(parent<Int>)<Int> ->
```

Create a grid layout on window `parent`. Returns a layout handle.

<a id="sym-qt_grid_add"></a>
### `qt_grid_add` · fn

```
qt_grid_add(layout<Int>, child<Int>, row<Int>, col<Int>)<Int> ->
```

Add widget `child` to grid-layout `layout` at (row, col). Returns 0.

<a id="sym-qt_group_create"></a>
### `qt_group_create` · fn

```
qt_group_create(parent<Int>, title<String>)<Int> ->
```

Create a titled group-box container under `parent`; put a layout on it. Returns its handle or 0.

<a id="sym-qt_hbox"></a>
### `qt_hbox` · fn

```
qt_hbox(parent<Int>)<Int> ->
```

Create a horizontal box layout on window `parent`. Returns a layout handle.

<a id="sym-qt_init"></a>
### `qt_init` · fn

```
qt_init()<Bool> ->
```

Initialize the QApplication (once); true if the GUI is available.

<a id="sym-qt_kind"></a>
### `qt_kind` · fn

```
qt_kind(h<Int>)<Int> ->
```

Static widget kind (QtWidgetKind) of handle `h`, or 0 if not a widget.

<a id="sym-qt_label_create"></a>
### `qt_label_create` · fn

```
qt_label_create(parent<Int>, text<String>)<Int> ->
```

Create a label with `text` under `parent` (0 = none); returns its handle or 0.

<a id="sym-qt_label_set_text"></a>
### `qt_label_set_text` · fn

```
qt_label_set_text(label<Int>, text<String>)<Int> ->
```

Replace label text. Returns 0, or -1 on a non-label handle.

<a id="sym-qt_label_text"></a>
### `qt_label_text` · fn

```
qt_label_text(label<Int>)<String> ->
```

Current label text (String); "" on a non-label handle.

<a id="sym-qt_layout_add"></a>
### `qt_layout_add` · fn

```
qt_layout_add(layout<Int>, child<Int>)<Int> ->
```

Add widget `child` to box-layout `layout`. Returns 0.

<a id="sym-qt_layout_add_layout"></a>
### `qt_layout_add_layout` · fn

```
qt_layout_add_layout(layout<Int>, sub<Int>)<Int> ->
```

Nest sub-layout `sub` inside box-layout `layout` (adds it as the next item). Returns 0.

<a id="sym-qt_layout_set_stretch"></a>
### `qt_layout_set_stretch` · fn

```
qt_layout_set_stretch(layout<Int>, index<Int>, stretch<Int>)<Int> ->
```

Set the stretch factor of the item at `index` (add order, 0-based) to `stretch`. Returns 0.

<a id="sym-qt_list_add"></a>
### `qt_list_add` · fn

```
qt_list_add(list<Int>, text<String>)<Int> ->
```

Append `text` as the last list item. Returns 0.

<a id="sym-qt_list_count"></a>
### `qt_list_count` · fn

```
qt_list_count(list<Int>)<Int> ->
```

List item count, or -1 on a bad handle.

<a id="sym-qt_list_create"></a>
### `qt_list_create` · fn

```
qt_list_create(parent<Int>)<Int> ->
```

Create an item list under `parent`; selection changes enqueue QtEvent::CurrentChanged. Returns its handle.

<a id="sym-qt_list_current"></a>
### `qt_list_current` · fn

```
qt_list_current(list<Int>)<Int> ->
```

Current (0-based) list index, or -1 when none / bad handle.

<a id="sym-qt_list_item_text"></a>
### `qt_list_item_text` · fn

```
qt_list_item_text(list<Int>, idx<Int>)<String> ->
```

Text of list item `idx` (String); "" on a bad handle/index.

<a id="sym-qt_list_set_current"></a>
### `qt_list_set_current` · fn

```
qt_list_set_current(list<Int>, idx<Int>)<Int> ->
```

Select list item `idx`. Returns 0.

<a id="sym-qt_main_window_create"></a>
### `qt_main_window_create` · fn

```
qt_main_window_create()<Int> ->
```

Create a top-level QMainWindow (kind Window) with menubar/statusbar/toolbar support; returns its handle or 0.

<a id="sym-qt_menu_add"></a>
### `qt_menu_add` · fn

```
qt_menu_add(mw<Int>, title<String>)<Int> ->
```

Add a top-level menu titled `title` to the menu bar; returns its menu handle or 0.

<a id="sym-qt_menubar"></a>
### `qt_menubar` · fn

```
qt_menubar(mw<Int>)<Int> ->
```

The main window's menu bar handle (created on demand).

<a id="sym-qt_msg_about"></a>
### `qt_msg_about` · fn

```
qt_msg_about(parent<Int>, title<String>, text<String>)<Int> ->
```

Show a modal 'about' box (blocks until dismissed). Returns 0.

<a id="sym-qt_msg_error"></a>
### `qt_msg_error` · fn

```
qt_msg_error(parent<Int>, title<String>, text<String>)<Int> ->
```

Show a modal critical-error message box (blocks until dismissed). Returns 0.

<a id="sym-qt_msg_info"></a>
### `qt_msg_info` · fn

```
qt_msg_info(parent<Int>, title<String>, text<String>)<Int> ->
```

Show a modal informational message box (blocks until dismissed). Returns 0.

<a id="sym-qt_msg_question"></a>
### `qt_msg_question` · fn

```
qt_msg_question(parent<Int>, title<String>, text<String>)<Int> ->
```

Show a modal Yes/No question. Returns 1 for Yes, 0 for No/dismiss.

<a id="sym-qt_msg_warn"></a>
### `qt_msg_warn` · fn

```
qt_msg_warn(parent<Int>, title<String>, text<String>)<Int> ->
```

Show a modal warning message box (blocks until dismissed). Returns 0.

<a id="sym-qt_on_event"></a>
### `qt_on_event` · fn

```
qt_on_event(handler<fn(Int, Int) -> Void>)<Int> ->
```

Register fn(handle, kind) called for each control event queued while qt_run() runs. Returns 0.

<a id="sym-qt_post_event"></a>
### `qt_post_event` · fn

```
qt_post_event(h<Int>, kind<Int>)<Int> ->
```

Enqueue a synthetic control event for handle `h` (thread-safe: a background async can signal the UI loop without touching widgets off-thread). Returns 0.

<a id="sym-qt_process_events"></a>
### `qt_process_events` · fn

```
qt_process_events()<Int> ->
```

Pump the Qt event loop once. Returns 0, or -1 if the GUI is not running.

<a id="sym-qt_progress_create"></a>
### `qt_progress_create` · fn

```
qt_progress_create(parent<Int>, max<Int>)<Int> ->
```

Create a progress bar with range 0..max under `parent`. Returns its handle or 0.

<a id="sym-qt_progress_set_value"></a>
### `qt_progress_set_value` · fn

```
qt_progress_set_value(bar<Int>, value<Int>)<Int> ->
```

Set the progress value (clamped). Returns 0.

<a id="sym-qt_quit"></a>
### `qt_quit` · fn

```
qt_quit()<Int> ->
```

Shut the GUI down (terminal - do not use Qt handles afterwards). Returns 0.

<a id="sym-qt_radio_checked"></a>
### `qt_radio_checked` · fn

```
qt_radio_checked(radio<Int>)<Bool> ->
```

true if the radio button is checked, else false.

<a id="sym-qt_radio_create"></a>
### `qt_radio_create` · fn

```
qt_radio_create(parent<Int>, text<String>)<Int> ->
```

Create a radio button under `parent`; toggles enqueue QtEvent::Toggled. Returns its handle.

<a id="sym-qt_radio_set_checked"></a>
### `qt_radio_set_checked` · fn

```
qt_radio_set_checked(radio<Int>, on<Bool>)<Int> ->
```

Check (true) / uncheck (false) the radio (enqueues QtEvent::Toggled). Returns 0.

<a id="sym-qt_rich_append"></a>
### `qt_rich_append` · fn

```
qt_rich_append(ed<Int>, text<String>)<Int> ->
```

Append `text` at the end (keeps the current character format). Returns 0.

<a id="sym-qt_rich_clear"></a>
### `qt_rich_clear` · fn

```
qt_rich_clear(ed<Int>)<Int> ->
```

Clear all rich-text content. Returns 0.

<a id="sym-qt_rich_create"></a>
### `qt_rich_create` · fn

```
qt_rich_create(parent<Int>)<Int> ->
```

Create a rich-text editor (QTextEdit) under `parent`; edits enqueue QtEvent::TextChanged.

<a id="sym-qt_rich_html"></a>
### `qt_rich_html` · fn

```
qt_rich_html(ed<Int>)<String> ->
```

Current rich-text body as HTML (String); "" on a bad handle.

<a id="sym-qt_rich_plain"></a>
### `qt_rich_plain` · fn

```
qt_rich_plain(ed<Int>)<String> ->
```

Current plain text (String); "" on a bad handle.

<a id="sym-qt_rich_set_html"></a>
### `qt_rich_set_html` · fn

```
qt_rich_set_html(ed<Int>, html<String>)<Int> ->
```

Set the rich-text editor body from `html` (supports <b>/<i>/<font color> etc). Returns 0.

<a id="sym-qt_rich_set_plain"></a>
### `qt_rich_set_plain` · fn

```
qt_rich_set_plain(ed<Int>, text<String>)<Int> ->
```

Set the rich-text editor's plain text (clears formatting). Returns 0.

<a id="sym-qt_rich_set_text_color"></a>
### `qt_rich_set_text_color` · fn

```
qt_rich_set_text_color(ed<Int>, r<Int>, g<Int>, b<Int>)<Int> ->
```

Set the editor's text color to (r,g,b) each 0-255. Returns 0.

<a id="sym-qt_run"></a>
### `qt_run` · fn

```
qt_run()<Int> ->
```

Run the Qt native event loop, dispatching queued control events to the qt_on_event handler, until qt_run_stop()/qt_quit(). Returns exit code.

<a id="sym-qt_run_stop"></a>
### `qt_run_stop` · fn

```
qt_run_stop()<Int> ->
```

Stop a running qt_run() loop (graceful; GUI stays up). Returns 0, or -1 if not running.

<a id="sym-qt_screen_dpi"></a>
### `qt_screen_dpi` · fn

```
qt_screen_dpi()<Int> ->
```

Primary screen device-pixel-ratio scaled to 96dpi (100 = 1.0x), or 100 if no screen.

<a id="sym-qt_screen_height"></a>
### `qt_screen_height` · fn

```
qt_screen_height()<Int> ->
```

Primary screen height in logical pixels, or -1 if no screen.

<a id="sym-qt_screen_width"></a>
### `qt_screen_width` · fn

```
qt_screen_width()<Int> ->
```

Primary screen width in logical pixels, or -1 if no screen.

<a id="sym-qt_set_timer"></a>
### `qt_set_timer` · fn

```
qt_set_timer(ms<Int>)<Int> ->
```

Arm a repeating timer every `ms` ms; poll qt_timer_fired(). Returns 0.

<a id="sym-qt_slider_create"></a>
### `qt_slider_create` · fn

```
qt_slider_create(parent<Int>, min<Int>, max<Int>)<Int> ->
```

Create a horizontal slider with range [min, max]; value changes enqueue QtEvent::ValueChanged.

<a id="sym-qt_slider_set_value"></a>
### `qt_slider_set_value` · fn

```
qt_slider_set_value(slider<Int>, value<Int>)<Int> ->
```

Set the slider value (clamped). Returns 0.

<a id="sym-qt_slider_value"></a>
### `qt_slider_value` · fn

```
qt_slider_value(slider<Int>)<Int> ->
```

Current slider value, or 0 on a bad handle.

<a id="sym-qt_spin_create"></a>
### `qt_spin_create` · fn

```
qt_spin_create(parent<Int>, min<Int>, max<Int>)<Int> ->
```

Create an integer spin box with range [min, max]; value changes enqueue QtEvent::ValueChanged.

<a id="sym-qt_spin_set_value"></a>
### `qt_spin_set_value` · fn

```
qt_spin_set_value(spin<Int>, value<Int>)<Int> ->
```

Set the spin box value (clamped). Returns 0.

<a id="sym-qt_spin_value"></a>
### `qt_spin_value` · fn

```
qt_spin_value(spin<Int>)<Int> ->
```

Current spin box value, or 0 on a bad handle.

<a id="sym-qt_statusbar_message"></a>
### `qt_statusbar_message` · fn

```
qt_statusbar_message(mw<Int>, text<String>)<Int> ->
```

Show `text` in the main window's status bar. Returns 0, or -1 on a bad handle.

<a id="sym-qt_statusbar_text"></a>
### `qt_statusbar_text` · fn

```
qt_statusbar_text(mw<Int>)<String> ->
```

Current status-bar message (String); "" on a bad handle.

<a id="sym-qt_tabs_add"></a>
### `qt_tabs_add` · fn

```
qt_tabs_add(tabs<Int>, text<String>)<Int> ->
```

Append a tab titled `text`; returns the page's widget handle (put a layout on it).

<a id="sym-qt_tabs_count"></a>
### `qt_tabs_count` · fn

```
qt_tabs_count(tabs<Int>)<Int> ->
```

Tab count, or -1 on a bad handle.

<a id="sym-qt_tabs_create"></a>
### `qt_tabs_create` · fn

```
qt_tabs_create(parent<Int>)<Int> ->
```

Create a tab container under `parent`; returns its handle or 0.

<a id="sym-qt_tabs_current"></a>
### `qt_tabs_current` · fn

```
qt_tabs_current(tabs<Int>)<Int> ->
```

Current (0-based) tab index, or -1 on a bad handle.

<a id="sym-qt_tabs_set_current"></a>
### `qt_tabs_set_current` · fn

```
qt_tabs_set_current(tabs<Int>, idx<Int>)<Int> ->
```

Select tab `idx` (enqueues QtEvent::CurrentChanged). Returns 0.

<a id="sym-qt_text_edit_create"></a>
### `qt_text_edit_create` · fn

```
qt_text_edit_create(parent<Int>)<Int> ->
```

Create a multi-line plain-text editor under `parent`; edits enqueue QtEvent::TextChanged. Returns its handle.

<a id="sym-qt_text_edit_set_text"></a>
### `qt_text_edit_set_text` · fn

```
qt_text_edit_set_text(ed<Int>, text<String>)<Int> ->
```

Replace editor text. Returns 0, or -1 on a non-editor handle.

<a id="sym-qt_text_edit_text"></a>
### `qt_text_edit_text` · fn

```
qt_text_edit_text(ed<Int>)<String> ->
```

Current editor text (String); "" on a bad handle.

<a id="sym-qt_timer_fired"></a>
### `qt_timer_fired` · fn

```
qt_timer_fired()<Bool> ->
```

true once the armed timer has fired since the last check (read clears).

<a id="sym-qt_toolbar_create"></a>
### `qt_toolbar_create` · fn

```
qt_toolbar_create(mw<Int>, title<String>)<Int> ->
```

Add a toolbar titled `title` to the main window; returns its handle or 0.

<a id="sym-qt_vbox"></a>
### `qt_vbox` · fn

```
qt_vbox(parent<Int>)<Int> ->
```

Create a vertical box layout on window `parent`. Returns a layout handle.

<a id="sym-qt_wait_event"></a>
### `qt_wait_event` · fn

```
qt_wait_event(timeout<Int>)<Bool> ->
```

Pump Qt until a control event (true) or `timeout` ms lapses (false); negative waits.

<a id="sym-qt_web_back"></a>
### `qt_web_back` · fn

```
qt_web_back(web<Int>)<Int> ->
```

Go back in history. Returns 0, or -1 on a bad handle.

<a id="sym-qt_web_create"></a>
### `qt_web_create` · fn

```
qt_web_create(parent<Int>)<Int> ->
```

gen_qt[mod_wrappers]: begin
Create a QWebEngineView under `parent`; returns its handle or 0 (needs QtWebEngine).

<a id="sym-qt_web_forward"></a>
### `qt_web_forward` · fn

```
qt_web_forward(web<Int>)<Int> ->
```

Go forward in history. Returns 0, or -1 on a bad handle.

<a id="sym-qt_web_load"></a>
### `qt_web_load` · fn

```
qt_web_load(web<Int>, url<String>)<Int> ->
```

Begin loading `url` in the web view (async). Returns 0, or -1 on a bad handle.

<a id="sym-qt_web_loading"></a>
### `qt_web_loading` · fn

```
qt_web_loading(web<Int>)<Bool> ->
```

true while the page is still loading.

<a id="sym-qt_web_reload"></a>
### `qt_web_reload` · fn

```
qt_web_reload(web<Int>)<Int> ->
```

Reload the page. Returns 0, or -1 on a bad handle.

<a id="sym-qt_web_title"></a>
### `qt_web_title` · fn

```
qt_web_title(web<Int>)<String> ->
```

Current page title (String); "" on a bad handle.

<a id="sym-qt_web_url"></a>
### `qt_web_url` · fn

```
qt_web_url(web<Int>)<String> ->
```

Current page URL (String); "" on a bad handle.

<a id="sym-qt_web_zoom_in"></a>
### `qt_web_zoom_in` · fn

```
qt_web_zoom_in(web<Int>)<Int> ->
```

Zoom the web view in (larger text). Returns 0, or -1 on a bad handle.

<a id="sym-qt_web_zoom_out"></a>
### `qt_web_zoom_out` · fn

```
qt_web_zoom_out(web<Int>)<Int> ->
```

Zoom the web view out (smaller text). Returns 0, or -1 on a bad handle.

<a id="sym-qt_widget_enabled"></a>
### `qt_widget_enabled` · fn

```
qt_widget_enabled(h<Int>)<Bool> ->
```

true while widget `h` is enabled, else false.

<a id="sym-qt_widget_set_enabled"></a>
### `qt_widget_set_enabled` · fn

```
qt_widget_set_enabled(h<Int>, on<Bool>)<Int> ->
```

Enable (true) / disable (false) any widget. Returns 0, or -1 on a non-widget handle.

<a id="sym-qt_widget_set_font_bold"></a>
### `qt_widget_set_font_bold` · fn

```
qt_widget_set_font_bold(h<Int>, on<Int>)<Int> ->
```

Toggle a widget's font bold (on != 0). Returns 0, or -1 on a bad handle.

<a id="sym-qt_widget_set_font_size"></a>
### `qt_widget_set_font_size` · fn

```
qt_widget_set_font_size(h<Int>, pt<Int>)<Int> ->
```

Set a widget's font point size. Returns 0, or -1 on a bad handle.

<a id="sym-qt_widget_set_text_color"></a>
### `qt_widget_set_text_color` · fn

```
qt_widget_set_text_color(h<Int>, r<Int>, g<Int>, b<Int>)<Int> ->
```

Set a widget's foreground text color via palette (r,g,b each 0-255). Returns 0, or -1 on a bad handle.

<a id="sym-qt_widget_set_visible"></a>
### `qt_widget_set_visible` · fn

```
qt_widget_set_visible(h<Int>, on<Bool>)<Int> ->
```

Show (true) / hide (false) any widget. Returns 0, or -1 on a non-widget handle.

<a id="sym-qt_widget_visible"></a>
### `qt_widget_visible` · fn

```
qt_widget_visible(h<Int>)<Bool> ->
```

true while widget `h` is visible, else false.

<a id="sym-qt_window_close"></a>
### `qt_window_close` · fn

```
qt_window_close(w<Int>)<Int> ->
```

Destroy window `w` (children go with it). Returns 0.

<a id="sym-qt_window_create"></a>
### `qt_window_create` · fn

```
qt_window_create()<Int> ->
```

Create a top-level window; returns its Int handle or 0 on failure.

<a id="sym-qt_window_height"></a>
### `qt_window_height` · fn

```
qt_window_height(w<Int>)<Int> ->
```

Content height in pixels, or -1 on a bad handle.

<a id="sym-qt_window_hide"></a>
### `qt_window_hide` · fn

```
qt_window_hide(w<Int>)<Int> ->
```

Hide (unmap) the window. Returns 0.

<a id="sym-qt_window_resize"></a>
### `qt_window_resize` · fn

```
qt_window_resize(w<Int>, width<Int>, height<Int>)<Int> ->
```

Resize to (width, height) pixels. Returns 0.

<a id="sym-qt_window_set_title"></a>
### `qt_window_set_title` · fn

```
qt_window_set_title(w<Int>, title<String>)<Int> ->
```

Set titlebar text. Returns 0, or -1 on a bad handle.

<a id="sym-qt_window_show"></a>
### `qt_window_show` · fn

```
qt_window_show(w<Int>)<Int> ->
```

Show (map) the window. Returns 0.

<a id="sym-qt_window_title"></a>
### `qt_window_title` · fn

```
qt_window_title(w<Int>)<String> ->
```

Current titlebar text (String); "" on a bad handle.

<a id="sym-qt_window_visible"></a>
### `qt_window_visible` · fn

```
qt_window_visible(w<Int>)<Bool> ->
```

true while window `w` is visible, else false.

<a id="sym-qt_window_width"></a>
### `qt_window_width` · fn

```
qt_window_width(w<Int>)<Int> ->
```

Content width in pixels, or -1 on a bad handle.
