// Simple select test

test_naked() -> {
    x<Int const> = 42;
    result<Int> = select(x) -> {
        1 -> 10,
        42 -> 420,
        ? -> 0
    };
    println(result);
}

main() -> {
    println("Starting test");
    test_naked();
    println("Done");
}
