# Auto-generated by `vyb bindgen --full` (libclang). Do not edit.
// @test: full preprocessor (libclang) bindings (fixture)
// @parse-only: true


share(all)
COUNT()<CInt> -> {
    return 4
}

share(all)
WIDE()<CInt> -> {
    return 8
}

share(all)
LIMIT_HI()<CInt> -> {
    return 0xFF00
}

share(all)
SQUARE(x<Int>)<Int> -> {
    return (x * x)
}

share(all)
ADD2(a<Int>, b<Int>)<Int> -> {
    return (a + b)
}

share(all)
MAX(a<Int>, b<Int>)<Int> -> {
    return select(((a > b))) -> { true -> (a), false -> (b) }
}

share(all)
CLAMP(v<Int>, lo<Int>, hi<Int>)<Int> -> {
    return select(((v < lo))) -> { true -> (lo), false -> (select(((v > hi))) -> { true -> (hi), false -> (v) }) }
}

share(all)
IS_EVEN(x<Int>)<Bool> -> {
    return ((x % 2) == 0)
}

share(all)
IS_POS(x<Int>)<Bool> -> {
    return (x > 0)
}

share(all)
MASK8(x<Int>)<Int> -> {
    return (x & 0xFF)
}

share(all)
LSL2(x<Int>)<Int> -> {
    return (x << 2)
}

share(all)
STATUS()<String> -> {
    return "ok"
}

share(all)
BIG_ENOUGH()<CInt> -> {
    return 1
}

share(all)
#[repr(C)]
struct Vec2 {
    x<CInt>,
    y<CInt>,
}

share(all)
enum Mode { MODE_A, MODE_B, MODE_C }

share(all)
compute(base<CInt>, n<CInt>)<CInt>

share(all)
add64(a<CULong>, b<CULong>)<CULong>

share(all)
classify(p<loc<Vec2>>)<Mode>

share(all)
scale(out<loc<Vec2>>, f<CInt>)<CVoid>

