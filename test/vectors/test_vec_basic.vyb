main()<Int> -> {
    // Test basic Vec creation
    numbers<Vec<Int>> = Vec();

    // Test push operations
    numbers.push(10);
    numbers.push(20);
    numbers.push(30);

    // Test println with Vec
    println(numbers);

    return 0;
}