// Test Vec(size) to isolate memory allocation issues

main()<Int> -> {
    // Test just the sized constructor
    sized_vec<Vec<Int>> = Vec(3);
    println("Sized Vec created successfully");

    return 0;
}