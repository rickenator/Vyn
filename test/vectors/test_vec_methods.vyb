main()<Int> -> {
    // Test Vec with different types
    words<Vec<String>> = Vec();
    words.push("hello");
    words.push("world");

    empty<Vec<Int>> = Vec();
    println("Length:", empty.len());

    empty.push(42);
    println("After push:", empty.len());
    println("Contents:", empty);

    return 0;
}