// Test that struct construction works correctly

struct Point {
    x<Int>,
    y<Int>
}

main()<Int> -> {
    // Using struct literal syntax
    p<Point> = Point { x = 10, y = 20 };
    return p.x + p.y;
}
