// Test: Valid MY ownership transfer into an owning binding - move is allowed
// @expect: pass
// @expect-return: 42
main()<Int> -> {
    x<my<Int>> = my(42)
    y<my<Int>> = x  // ownership transfers from x to y
    return y        // reading y yields the underlying value
}
