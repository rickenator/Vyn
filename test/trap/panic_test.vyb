# Semantic Test: Panic statement validation
// @expect: fail

critical_failure(condition<Bool>)<Int> -> {
    if (condition) {
        panic("Critical system failure!")
    }
    return 0
}

main()<Int> -> {
    result<Int> = critical_failure(true)
    return result
}
