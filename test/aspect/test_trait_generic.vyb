// @test: generic aspect bind to Vec execution
// @description: Generic aspect binds for Vec<T> monomorphize and execute through dot-call dispatch
// @category: aspect, generics, vec
// @expect: pass
// @expect-return: 3

// Phase 3: Generic aspect implementations
// Tests bind<T> Trait -> GenericType<T>

aspect Container {
    size(self)<Int> -> { }
    is_empty(self)<Bool> -> { }
}

// Generic bind: any Vec<T> implements Container
bind<T> Container -> Vec<T> {
    size(self)<Int> -> {
        return self.len()
    }

    is_empty(self)<Bool> -> {
        return self.len() == 0
    }
}

main()<Int> -> {
    // Create a Vec<Int>
    numbers<Vec<Int>> = Vec()
    numbers.push(1)
    numbers.push(2)
    numbers.push(3)

    // Call aspect methods on Vec<Int>
    size<Int> = numbers.size()
    empty<Bool> = numbers.is_empty()

    // Should return 3 (size is 3, empty is 0/false)
    return size
}
