// @test: builtin Vec smoke fixture
// @description: Current Vyb uses builtin Vec<T>; this replaces the obsolete hand-rolled non-generic Vec fixture
// @category: vec, aspect-fixture
// @expect: pass
// @expect-return: 2

main()<Int> -> {
    values<Vec<Int>> = Vec()
    values.push(10)
    values.push(20)
    return values.len()
}
