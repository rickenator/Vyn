// Basic aspect declaration test
// Tests Phase 1: Trait declarations with method signatures
// @expect-return: 42

aspect Printable {
    print(self<Self>) -> { }
    to_string(self<Self>)<String> -> { }
}

aspect Comparable {
    lt(self<Self>, other<Self>)<Bool> -> { }
    eq(self<Self>, other<Self>)<Bool> -> {
        // Default implementation
        return !(self.lt(other)) && !(other.lt(self));
    }
}

main()<Int> -> {
    return 42;
}
