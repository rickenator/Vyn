# Test type parameter usage in method bodies

aspect Display {
    show(self<Self>) -> { }
}

struct Box<T> {
    value<T>
}

# Generic bind where methods can use T
bind<T> Display -> Box<T> {
    show(self<Box<T>>) -> {
        # T should be recognized as a valid type parameter here
        # For now, just return without using T
        return
    }
}

main()<Int> -> {
    return 0
}
