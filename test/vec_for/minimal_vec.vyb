// Minimal Vec iteration test
// @expect-return: 5
import collections
main()<Int> -> {
    v<Vec<Int>> = Vec();
    v.push(5);

    s<Int> = 0;
    for (x in v) {
        s = s + x;
    }

    return s;
}
