// Simple Vec iteration without conditions
// @expect-return: 6
main()<Int> -> {
    v<Vec<Int>> = Vec();
    v.push(1);
    v.push(2);
    v.push(3);

    var<Int> sum = 0;
    for (x in v) {
        sum = sum + x;
    }
    return sum;  // Should return 6 (1+2+3)
}
